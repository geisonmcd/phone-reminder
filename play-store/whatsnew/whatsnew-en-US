Fixes Google Drive backup so sign-in and release builds sync reminders reliably.

- Requires Drive permission before reusing a Google account
- Recovers expired Drive authorization
- Keeps Drive API classes intact in release builds
